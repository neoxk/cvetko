import type { NotificationChannel } from './types';

export const NOTIFICATION_CHANNELS: NotificationChannel[] = ['care', 'weather'];

export const DEFAULT_NOTIFICATION_CHANNEL: NotificationChannel = 'care';

export const DEFAULT_TASK_NOTIFICATION_HOUR_UTC = 8;

export const WEATHER_ALERTS_ENABLED = false;
